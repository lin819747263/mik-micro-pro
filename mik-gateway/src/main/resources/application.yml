spring:
  cloud:
    gateway:
      routes:
        - id: user_router
          uri: lb://admin-service
          predicates:
            - Path=/u/**
          filters:
#            - PrefixPath=/u
            - StripPrefix=1
